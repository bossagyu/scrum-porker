# README 改善デザイン

日付: 2026-02-15
アプローチ: 構造重視のリファイン（既存構成を活かしつつ改善）

## 目的

READMEを総合的にレベルアップする。見た目と内容の両面を改善し、プロジェクトの魅力を伝える。

## 改善内容

### 1. ヘッダー部分

- Vercelデプロイバッジ、MITライセンスバッジを追加
- キャッチコピーに「アカウント登録不要」を明記
- スクリーンショットのプレースホルダーを配置（`docs/images/screenshot-room.png`）

### 2. 機能一覧

既存の8項目に加え、最近実装された機能を3つ追加:

- 招待・共有（QRコード / URLコピー / Web Share API）
- ダークモード（ライト / ダーク / システム設定）
- 多言語対応（日本語・英語、next-intl）

### 3. 技術スタック

テーブルに `next-intl`（多言語）と `next-themes`（ダークモード）を追加。

### 4. セットアップ・コマンド一覧

変更なし（現状で十分整理されている）。

### 5. プロジェクト構成

- `messages/` ディレクトリ（i18n翻訳ファイル）を追加
- `components/layout/` の説明にテーマ切替を補足

### 6. データベース

テキストのER図をMermaid ER図に置き換え、視覚的に分かりやすくする。

### 7. ライセンス

変更なし。

## 変更しない部分

- 言語: 日本語のまま
- 全体構成: セクション順序は維持
- コマンド一覧: 現状のまま
- セットアップ手順: 現状のまま

## スクリーンショット

`docs/images/` ディレクトリを作成し、プレースホルダーコメントを配置。
ユーザーが後から実際の画像を差し替えられるようにする。
